setHeader <- function(wb, text, position = "center") {
  .Deprecated("setHeaderFooter()")
  stop()
}

setFooter <- function(wb, text, position = "center") {
  .Deprecated("setHeaderFooter()")
  stop()
}

